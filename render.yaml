services:
  - type: web
    name: analizador-video
    runtime: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
    disk:
      name: uploads
      mountPath: /data
      sizeGB: 5
